# Install the shared HSM test framework from GitLab
# Replace the URL with your actual GitLab repository URL
hsm-test-framework @ git+https://gitlab.yourcompany.com/qa/hsm-test-framework.git

# Optional: Windows UI automation
pywinauto>=0.6.8; sys_platform == "win32"

# Optional: Kiwi TCMS
tcms-api>=12.0

# Optional: Grafana metrics
prometheus-client>=0.20.0
