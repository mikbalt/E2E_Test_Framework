# Install the framework itself (editable mode for development)
-e .

# .env file support (auto-load TCMS credentials etc.)
python-dotenv>=1.0.0

# Kiwi TCMS integration
tcms-api>=12.0

# Optional: UI automation (Windows only)
pywinauto>=0.6.8; sys_platform == "win32"

# Optional: Grafana/Prometheus metrics
prometheus-client>=0.20.0

# Optional: Loki remote log collection
requests>=2.28.0

# Optional: HTML report (alternative to Allure)
pytest-html>=4.1.0
